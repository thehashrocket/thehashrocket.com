{
    "scripts": {
        "setup": ". \"$NVM_DIR/nvm.sh\" 2>/dev/null; nvm use 2>/dev/null; cp $CONDUCTOR_ROOT_PATH/.env.local .env.local && pnpm install --frozen-lockfile",
        "run": ". \"$NVM_DIR/nvm.sh\" 2>/dev/null; nvm use 2>/dev/null; pnpm run dev",
        "archive": ""
    }
}
